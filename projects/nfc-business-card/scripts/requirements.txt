# Python packages used by the committed scripts in this folder.
#   openpyxl      - check-e16-manufacture.py (BOM / pick-and-place sheets)
#   Pillow, numpy - generate-*-svg.py, render-stl-iso.py, planning checks
# FreeCAD ships its own interpreter (freecadcmd) and is not installed here.
openpyxl>=3.1
Pillow>=10
numpy>=1.26
matplotlib>=3.8
shapely>=2.1,<3

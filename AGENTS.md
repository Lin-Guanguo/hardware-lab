# Agent 工作指引

## 范围与上下文

- 本仓库在 `projects/` 下管理多个独立项目。
- 修改前先阅读根目录 README 和目标项目的 README；项目内如有更具体的 `AGENTS.md`，也需遵循。
- 换机器或换会话接手时先看[环境与常驻服务](docs/environment.md)：工具链路径、EDA 桥接（launchd，按需启动）的从零重建、五分钟自检清单与常见故障；项目进度与待办看所属项目的进度看板（如[电子名片进度](projects/nfc-business-card/docs/progress.md)）。
- 围绕当前任务修改目标项目，仅在需要时调整共享文件，保留无关的已有工作。
- 根文档负责导航和通用约定；实现细节、可用命令及下一步放在所属项目中。
- 本仓库维护的 README 首个章节统一为“当前确定方案”，写清当前采用的方向、最新文件入口和待验证项；方案变化时同步更新相关目录，历史版本放在后续章节，不把选定方向写成已完成制造或实测。

## 目录约定与惰性初始化

- 统一目录名是分类约定，实际写入第一份有用内容时才创建对应目录，不预先生成整套目录树。
- 新项目可先只有 README。不要用 `.gitkeep`、空 README 或仅说明“暂无内容”的 README 为未来功能占位。
- 各项目按需要采用以下一级子目录；README 的目录树和链接只列已经存在的内容。

| 目录 | 创建时机与用途 |
| --- | --- |
| `firmware/` | 加入第一个固件工程时创建，保存源码和可复现配置 |
| `hardware/` | 有 BOM、接线表、原理图或 PCB 内容时创建 |
| `enclosure/` | 开始记录外壳设计或制作模型时创建 |
| `docs/` | 有项目专属资料、实验记录或设计文档时创建 |
| `scripts/` | 有实际可用的项目脚本时创建 |
| `wiring-diagrams/` | 生成第一张接线图时创建，保存独立 HTML、图册 README 和 `sources/` 图源 |

- 嘉立创 EDA 的原始 `.eprj2` 工程统一直接保存在仓库根目录 `eda/`，不使用工程文件符号链接；当前工程放在该目录，历史工程归档到 `eda/archive/`。正式工程纳入 Git，并从所属项目文档关联；BOM、接线说明等仍放在项目的 `hardware/`。保存并关闭工程后再移动或提交数据库文件，临时测试与 SQLite 日志不入库。

## 语言约定

- 后续新增或修改的文档默认使用中文，包括 README、AGENTS.md、使用说明、器件清单、设计文档和实验记录，方便用户阅读。
- 代码、标识符、代码注释和 docstring 使用英文；脚本和配置文件中的注释也保持英文。
- 专有名词、产品名、技术名词保留英文原文，例如 ESP32-S3、ESP-IDF、GPIO、SPI、BSP 和 LVGL；必要时补充中文解释。
- 文件名、目录名、命令、API 名称和配置键保持原样，不为翻译文档而改名。
- Git commit message 继续使用英文。
- 上游许可证和直接引用的原始资料保留原文；本仓库撰写的说明使用中文。

## 开发方式

- 优先采用简单可运行的实现，以及在 macOS 上可通过 shell 操作的工作流。
- 编写新驱动或引入其他框架前，先复用项目已有例程和 BSP。
- 每个固件工程应能独立构建，固定复现所需的工具链和依赖版本，不强求无关项目使用相同版本。
- 多个项目确实需要时再提取共享组件或脚本；有实际内容时再创建目录。
- 保留上游许可证，记录导入源码的 URL 和 revision；不要将嵌套的 `.git` 目录复制进本仓库。
- 工作分支使用 `lgg/` 前缀；只有用户要求时才 push 或发布。

## CAD 自动化

- FreeCAD 建模、参数修改、几何检查、保存和几何导出优先用 Python API / `freecadcmd`；颜色、透明度、可见性与视角用 GUI 环境中的 `FreeCADGui` API，不把需要 GUI 等同于必须模拟鼠标。
- Computer Use 主要用于最终视觉检查及无可用接口的操作；菜单反复失败时及时切换脚本入口，复用现有宏，不为此引入额外 MCP 或常驻服务。
- 重新生成前检查已有文件和手工/未保存修改；不能确认时输出到新目录，禁止覆盖。几何检查与视觉检查分别报告。可复现命令、环境与限制见[电子名片软件说明](projects/nfc-business-card/docs/software.md#freecad-脚本工作流)。

## 交互固件经验复用

- 开发按键与显示交互时，分别验证输入是否及时记录、界面是否及时更新。时间敏感采集避免依赖慢速绘图或日志完成；按复杂度选短循环、状态机或独立任务，共享状态的临界区保持短小。
- 优先局部重绘；分别测量采样间隔、绘图、日志发送与主循环的耗时峰值。验证需包含串口正常读取和停止读取，避免读取日志时掩盖缓冲堵塞。
- 调试日志采用有界、允许跳过的非阻塞输出；重要业务数据另行设计确认、重试或错误反馈。不要只靠扩大缓冲解决持续背压，也不要照搬本例的采样周期和消抖参数。
- 已验证案例、代码入口和测量方法见 [Image Oracle 响应性知识笔记](projects/image-oracle/docs/responsive-input-display.md)。

## 硬件事实与验证

- 区分宣传规格、用户自述购买、实物到货和实测结果；不要把假设写成引脚表，也不要将未经测试的功能标为可用。
- 提出接线方案或修改硬件配置前，确认具体板卡版本、电压要求和引脚占用。
- 改布局、布线、板框或器件位号前，先看 [PCB 设计规则索引](docs/pcb-design-rules.md)（DRC 看不见的规则、阈值、一手来源与本板适用性），并按 `pcb-methodology` skill 的敏感件门禁判断要动的器件属于哪一类：自由区可直接为美观或装配移动，带规则的必须附命名证据，天线净空/连接器基准/板框缺口必须重新推导。**原生 DRC 只证明几何没撞上，不证明设计正确**——已发生过"电容被挪到离服务引脚 24 mm、四项检查全过"的实例。
- 规则要挂一手来源。器件级规则引数据手册的 layout 章节或厂商应用笔记，通用规则引 [docs/pcb-design-rules.md](docs/pcb-design-rules.md) 里的来源列；**只有经验值而没有来源的规则，要么补来源，要么显式标为待验证假设**，不要当成既成事实执行。
- 区分固件构建与烧录。仅在当前任务授权设备烧录、且已确认目标设备和恢复方式后烧录。
- 开发范围限于弱电部分。熔岩灯保留原装市电供电，与实验电子模块分开放置。
- 执行与改动相关的检查，报告命令和结果；如果只完成构建验证或缺少实物，需明确说明。
- 有实质进展后，更新项目 README 的当前状态和下一步；接线、测量数据及详细发现放入项目文档。

## 接线图约定

- 用户已确认偏好“实物主板外形 + 完整排针丝印 + 模块端数字序号”的交互式 HTML 接线图，后续默认沿用此形式；简单共地和面包板说明也保持相同视觉风格。
- 有接线图的项目统一使用一级子目录 `projects/<项目名>/wiring-diagrams/`，保存可独立打开的 HTML；可编辑的对话图源放在其 `sources/` 子目录，目录 README 维护图册、适用板卡和验证状态。随第一张图一起创建，不为其他项目预建占位图册。
- 图中标明正反面和摆放方向，按实物排针顺序编号；相同圈号对应同一信号，不把 GPIO 编号与物理针序混用。只高亮本次接线，未接针脚仍保留丝印。
- 先核对板卡、电压和固件引脚配置，再绘图；已验证的接线不能为了排布整齐而擅自调整。共地分支、杜邦线端头和暂不接的针脚需要清楚标注。
- 在对话中提供或修改接线图时，同步归档源文件和独立 HTML，并更新项目图册索引；不要只留在临时目录或对话附件中。
- 具体画法、导出命令和检查方法见 [接线图制作与归档方法](docs/wiring-diagrams.md)。

## 文件与凭据

- 外部上游仓库统一放在根目录 `upstreams/`，一律用 Git submodule 引入，不安装到全局，也不复制一份上游源码。`.gitmodules` 中设 `branch = main` 表示按需跟随上游主线：需要更新时运行 `git submodule update --remote <path>`，父仓库会一并记录当次实际提交，验证记录里写明"验证于某次提交"。上游的主线随时会动，因此**判断依据始终是本仓库验证过的那个提交**，不是"最新版"。
- 上游分两类，不要混淆：**可直接调用的 Skill** 用 `.agents/skills/<name>` 的相对符号链接指向 `upstreams/` 对应目录；**只作参考资料的上游**（假设别的工具链、或会与自建桥接冲突）只放 `upstreams/`，不加符号链接、不执行其脚本，由本仓库自建的总控 Skill 引用。判断标准是"我们会不会真的去执行它"，不是它写得多好、星数多高。
- 本仓库自建的 Skill 是 `.agents/skills/` 下的**真实目录**（不是符号链接），随仓库一起版本管理。它负责编排流程、门禁与上游引用，**不复制上游的规则原文**，避免分叉维护。现有：[pcb-methodology](.agents/skills/pcb-methodology/SKILL.md)。
- `**/artifacts/**` 默认全部忽略，只保留各项目的 `artifacts/README.md` 索引（说明布局、命名规则与重建方式）。需要长期引用、文档会链接的**小证据**（重开/DRC/网表记录、检查脚本、渲染图册、报告）搬进受版本管理的目录，例如项目的 `hardware/records/`、`enclosure/`、`enclosure/renders/`；大块或可重建的产物留在被忽略的 `artifacts/` 里。
- 保持子模块的上游文件不变；本仓库的适配说明、安装命令和验证记录写在所属项目文档或本文件。初始化使用 `git submodule update --init --recursive`；用 `git submodule status` 查看每个上游当前锁定的提交，跟随主线更新后要重新验证受影响的部分，并把新提交记进验证记录。上游许可证随子模块保留，引用其内容时标注仓库、文件与规则编号。
- 使用 `easyeda-api` 时，上游示例中的 `${CLAUDE_SKILL_DIR}` 表示当前 Skill 所在目录；Codex 应根据加载路径解析，不假定该变量已设置。此版本已包含 API 参考文档，无需执行上游 README 中已不适用的 `build:docs` 命令。
- Git 保存源码和可复现配置；构建产物、缓存、临时固件和串口日志不入库。
- 凭据和本地连接配置不入库；需要配置示例时使用占位值。
- 长篇讨论留在 CyberMnema，通过链接关联；不将购物截图复制进本仓库。
